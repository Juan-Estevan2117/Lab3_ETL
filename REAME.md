# Lab 3: ETL

## Importante

Es necesario crear un archivo .env en la raiz del proyecto donde se incluyan los siguientes campos

DB_USER = '<user>'
DB_PASSWORD = '<password>'
DB_HOST= '<host>'  # default: localhost
DB_PORT = '<port>' # default: 3306 
DB_NAME = 'etl_lab_3'